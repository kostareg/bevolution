[package]
name = "bevolution"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.16.1"
rand = { version = "0.9.2", features = ["thread_rng"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
